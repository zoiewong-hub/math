# Sudoku Pro（全能数独软件）

一个纯前端的数独应用，支持：

- 用户注册 / 登录（本地离线账号）
- 不同难度新局（简单 / 中等 / 困难）
- 笔记模式（候选数标记）
- 冲突高亮与数据标识
- 错误次数限制、提示功能
- 计时、进度、动态得分
- 每个用户独立的历史得分记录

## 启动方式

```bash
python -m http.server 8000
```

浏览器打开：`http://localhost:8000`

> 数据会保存在浏览器 `localStorage` 中。
